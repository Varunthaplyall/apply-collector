# Vercel Python runtime requires requirements.txt (pyproject.toml is not read).
# Keep this minimal — Vercel free tier has a 250 MB unzipped function size limit.
# Excluded: playwright (~300MB), sentence-transformers+torch (~800MB+), pgvector (unused).
flask>=3.0,<4
psycopg2-binary>=2.9,<3
supabase>=2.0,<3
pyjwt>=2.9,<3
python-dotenv>=1.0,<2
pydantic>=2.0,<3
httpx>=0.27,<1
beautifulsoup4>=4.12,<5
lxml>=5.0,<6
feedparser>=6.0,<7
numpy>=1.24,<3
tenacity>=8.0,<10
